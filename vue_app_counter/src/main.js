import { createApp } from 'vue'
import App from './App.vue'         // it's Create App.vue File as a vue Application 

createApp(App).mount('#app')        // it's Mount Application on your HTML file in id = "app"

